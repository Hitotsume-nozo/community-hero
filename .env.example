# Copy to .env and fill in. The app runs WITHOUT a key (deterministic AI fallback),
# but real Gemini unlocks multimodal photo analysis and live insights.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
PORT=8080
